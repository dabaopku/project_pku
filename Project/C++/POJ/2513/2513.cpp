#include "stdio.h"
#include "iostream"
#include "string"
#include "map"
using namespace std;
int main()
{
	int col[130000]={0};
	map<int,char *> colname;
	char head[11],tail[11];
	while(scanf("%s%s",head,tail)){
		if(colname.count())
	}
}